pipeline {
  agent any
  stages{
    stage("Run images") {
       steps {
          sh 'docker rm -f $(docker ps -a -q)'
          sh 'docker rmi -f $(docker images -a -q)'
          sh 'docker-compose up -d'
       }
    }
  }
}