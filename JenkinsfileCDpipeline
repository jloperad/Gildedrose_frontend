pipeline {
  agent any
  stages{
    stage("Run images") {
       steps {
          sh 'docker rm -f $(docker ps -a -q)'
          sh 'docker container prune -f'
          sh 'docker-compose up -d'
       }
    }
  }
}